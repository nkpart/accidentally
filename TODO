* Do a fuzzy search. Methods that return things of the same type as the expected thing should be found.
* Create a type to wrap the returned method.
* Consider another syntax - instead of HowDoes.how_does("dog").become(3) we could have HowDoes.how_does "dog" => 3
